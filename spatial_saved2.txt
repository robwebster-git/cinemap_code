DECLARE
  dist NUMBER;
  geoma SDO_GEOMETRY;
  geomb SDO_GEOMETRY;

BEGIN

SDO_GEOM.SDO_CLOSEST_POINTS(s1987402.shops.geom, s1434165.cinemas.geom, 0.005, NULL, dist, geoma, geomb);

DBMS_OUTPUT.PUT_LINE('dist output parameter value = ' || dist);
END;
/
